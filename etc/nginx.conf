user www-data;
worker_processes 4;
pid /run/nginx.pid;


events {
       worker_connections 768;
}


server {
	listen 80 ;
	location / {
		proxy_pass 0.0.0.0:8080;
		}
	#location / { 
	#return 404;
	#	}
	#location ^~ /uploads/ {
	#	 root /home/box/web;
	#	}
	#location ~* \..+$ {
	#	 root /home/box/web/public;
	#	}
	location ^~ /hello/ {
		proxy_pass 0.0.0.0:8080; 
		}
	}