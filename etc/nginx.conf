server {
	listen 80;
	#location / {proxy_pass 127.0.0.1:8000;}
	location / { return 404}
	location ~* ^\/uploads\/.+ { root /home/box/web;}
	location ~* \..+$ { root /home/box/web/public;}
	location ^~ /hello/ {proxy_pass 127.0.0.1:8080; }
	}
#server {

#  listen 80 default;

#  location ^~ /uploads/ {
 #   root /home/box/web;
  #}

#  location ~* ^.+\.\w+$ {
#    root /home/box/web/public;
 # }

 # location / {
   # return 404;
  #}
  
#}